name: pos_client_final
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  bloc: ^8.1.4
  cloud_firestore: ^4.14.0
  dartz: ^0.10.1
  dio: ^5.6.0
  equatable: ^2.0.5
  firebase_auth: ^4.16.0
  firebase_core: ^2.24.2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  flutter_paypal: ^0.2.0
  foundation: ^0.0.5
  http: ^0.13.6
  localstorage: ^4.0.1+4
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
